spring:
  application:
    name: batch-backend

  #consul configuration, utilisation de spring consul config aussi, donc dans bootstrap
  cloud:
    consul:
      discovery:
        tags: backend
      host: localhost
      port: 8500